import React from "react";

const Calendar = () => {
  return (
    <section className="font-notokr text-text-base flex h-screen w-full flex-col items-center justify-center gap-8 bg-purple-300 p-5">
      캘린더
    </section>
  );
};

export default Calendar;
