import React from "react";

const Gallery = () => {
  return (
    <section className="font-notokr text-text-base flex h-screen w-full flex-col items-center justify-center gap-8 bg-orange-300 p-5">
      갤러리
    </section>
  );
};

export default Gallery;
